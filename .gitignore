# Dependencies — these are installed locally, never committed
node_modules/
# Environment variables — NEVER commit these
.env
.env.local
# Build output
dist/
build/
# OS files
.DS_Store
# Server service account key — this file contains sensitive credentials and should never be committed
server/serviceAccountKey.json